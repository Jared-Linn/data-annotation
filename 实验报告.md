# 实验报告：基于NLP的心理咨询对话自动分类标注

## 一、实验目的

1. 掌握NLP文本分类的完整流程：数据 → 标注 → 训练/评估 → 迭代优化
2. 理解心理咨询对话文本的特点，建立三级分类（S1/S2/S3）标注规范
3. 能够使用传统机器学习模型实现自动分类标注
4. 掌握多模型一致性校验、人机协同标注等质量提升方法
5. 形成规范可复用、可审计、可复现模型训练的完整标注数据集

## 二、实验环境与数据

### 2.1 实验环境

| 项目 | 配置 |
|------|------|
| 操作系统 | Windows Server 2022 |
| Python版本 | 3.13 |
| 分词工具 | jieba |
| 特征提取 | TF-IDF（1-gram, sublinear tf, max_features=10000） |
| 机器学习库 | scikit-learn |
| 分类模型 | LogisticRegression / MultinomialNB / LinearSVC |

### 2.2 数据来源

数据来自 `psy525` 心理咨询平台真实问答对话，共3份JSON文件（No-01~No-03），每份8366条。

**数据格式：**
- `question_title` — 用户提问标题
- `question_content` — 用户提问内容
- `answers[].dialogs[].content` — 咨询师与用户的多轮对话
- `labels.label` — 标注结果（待填）

### 2.3 标签体系

| 层级 | 含义 | 子类数 | 说明 |
|------|------|--------|------|
| **S1** | 日常困扰（轻度·普通心理困扰） | 17类（1.1~1.17） | 学业、职场、家庭、失眠、压力、社交等生活话题，可自主调节 |
| **S2** | 心理疾病（中度·疑似病理症状） | 9类（2.1~2.9） | 抑郁、焦虑、PTSD、强迫等需专业干预，**所有标签均为疑似判定** |
| **S3** | 紧急危机（重度·即刻人工干预） | 5类（3.1~3.5） | 自杀、自残、伤害他人等需立即干预 |

**标注原则：** S3 > S2 > S1 优先级覆盖，每条数据仅标一个最匹配标签。

## 三、实验过程

### 3.1 数据加载与预处理

**步骤：**
1. 加载JSON数据，读取 question_title / question_content / dialogs
2. 去重（基于文本相似度）
3. 清洗：去除空白字符、非中文字符
4. 构建完整文本：拼接标题 + 内容 + 所有对话

```python
def clean(text):
    text = re.sub(r'\s+', '', text)
    text = re.sub(r'[^一-鿿\w]', '', text)
    return text
```

### 3.2 特征工程

**分词：** 使用 jieba 分词

**去停用词：** 自定义中文停用词表（含常用虚词、助词、连词等）

**向量化：** TF-IDF（1-gram, sublinear tf, max_features=10000, min_df=1）

### 3.3 人工标注

基于标注规范（详见 `data/人工标注/annotation-specification.md`），对 No-01 随机抽取的3000条样本进行人工分类标注。

**人工标注分布：**

| 层级 | 数量 | 占比 |
|------|------|------|
| S1 | 2369 | 79.0% |
| S2 | 437 | 14.6% |
| S3 | 194 | 6.5% |
| **合计** | **3000** | **100%** |

覆盖全部31个子类（31/31），S3全部5个子类（5/5）。

### 3.4 模型训练与迭代

#### 3.4.1 初始模型（半监督基线）

直接用3000条人标数据训练 LogisticRegression（31类直接分类）。

| 指标 | 值 |
|------|-----|
| 测试准确率 | 45.1% |
| 全量类覆盖 | 31/31 |
| S3子类覆盖 | 5/5 |
| 全量S1/S2/S3分布 | 77.4% / 16.5% / 6.0% |

#### 3.4.2 人工修正迭代（方案C）

使用Web标注工具对低置信样本进行在线修正，共修正3351条标签。

**修正流程：**
1. 模型预测全量 → 计算置信度
2. 按置信度排序，低置信优先
3. 分批（每批250条）使用Web工具人工修正
4. 加权重训（修正样本 weight=5~15）
5. 重复迭代

**修正前后分布对比：**
```
        修正前    修正后(差异)
1.1:    366      136 (-230)     ← 模型过估学业类
1.9:    726      396 (-330)     ← 模型过估亲密关系
1.7:     91      377 (+286)     ← 模型低估压力类
2.1:    104      320 (+216)     ← 模型低估抑郁类
```

#### 3.4.3 两阶段分类架构

为解决31类直接分类准确率低的问题，采用**两阶段分类**：

```
Stage 1: S1/S2/S3 层级分类（3类）→ 准确率 81%
                    ↓
Stage 2: 按层级分具体子类
  S1 → 17类 (49%)   S2 → 9类 (67%)   S3 → 5类 (62%)
                    ↓
S3 关键词兜底 + 稀有类关键词补全
```

### 3.5 多模型对比

在 Stage 1（S层级分类）上对比三种模型：

| 模型 | 准确率 | S1 F1 | S2 F1 | S3 F1 | 特点 |
|------|--------|-------|-------|-------|------|
| **LogisticRegression** | **81.06%** | **0.88** | **0.61** | **0.43** | 最均衡，S3能分到 |
| MultinomialNB | 80.97% | 0.89 | 0.49 | 0.00 | S3完全丢失 |
| LinearSVC | 81.33% | 0.89 | 0.57 | 0.19 | S3很低 |

**结论：** LogisticRegression 因 `class_weight='balanced'` 能更好处理不平衡数据，S3的F1达到0.43，选为最终模型。

## 四、实验结果

### 4.1 各版本迭代对比

| 版本 | S1 | S2 | S3 | 类覆盖 | S3子类 |
|------|-----|-----|-----|--------|--------|
| 半监督基线 | 77.4% | 16.5% | 6.0% | 31/31 | 5/5 |
| w=15 修正 | 79.2% | 18.5% | 2.4% | 31/31 | 5/5 |
| w=5 修正 | 78.0% | 19.3% | 2.6% | 31/31 | 5/5 |
| w=5+关键词兜底 | 73.9% | 17.4% | 8.7% | 31/31 | 5/5 |
| **两阶段 + 最终版** | **72.3%** | **18.9%** | **8.8%** | **31/31** | **5/5** |

### 4.2 最终模型评估

#### Stage 1 分类报告

```
              precision    recall  f1-score   support
     S1       0.91      0.86      0.88       854
     S2       0.55      0.68      0.61       219
     S3       0.52      0.37      0.43        41
accuracy                           0.81      1114
```

#### Stage 2 分类报告

**S1（17类）准确率 50.8%：**
```
             precision    recall  f1-score
1.1 学业       0.79      0.63      0.70
1.6 失眠       0.61      0.88      0.72
1.9 亲密关系    0.76      0.56      0.65
1.13 低自尊    0.45      0.48      0.46
1.7 压力       0.41      0.32      0.36  ← 易混淆
```

**S2（9类）准确率 66.7%：**
```
             precision    recall  f1-score
2.1 抑郁       0.73      0.76      0.75
2.7 强迫       0.76      0.69      0.72
2.3 双相       1.00      0.75      0.86
```

**S3（5类）准确率 61.9%：**
```
             precision    recall  f1-score
3.2 自杀计划    0.59      0.90      0.72
3.5 报复       0.75      0.60      0.67
3.1 正在自杀    0.00      0.00      0.00  ← 样本太少
```

### 4.3 最终三份数据分布

| 文件 | S1 | S2 | S3 | 类 | S3子类 |
|------|-----|-----|-----|------|---------|
| **No-01** | 72.3% | 18.9% | 8.8% | 31/31 | 5/5 |
| **No-02** | 71.9% | 19.3% | 8.8% | 31/31 | 5/5 |
| **No-03** | 71.1% | 19.1% | 9.7% | 31/31 | 5/5 |

三份数据分布一致，无显著偏差。

### 4.4 特征实验对比

| 特征方案 | 准确率 |
|----------|--------|
| word 1-gram | **81.15%** |
| word 1-3gram | 80.52% |
| char 2-4gram | 78.19% |
| word + char 混合 | 79.71% |

**结论：** word 1-gram 最优，n-gram和char特征未带来提升。分析原因是中文分词后1-gram已能捕获足够信息，更高阶n-gram引入噪声。

## 五、Dialog Tags 标注（加分项）

对对话中的每条 dialog 内容，基于关键词规则标注三种标签：

| Tag | 含义 | 数量 | 占比 |
|-----|------|------|------|
| knowledge | 专业知识、引导来访者 | 93,975 | 73.7% |
| meaningless | 寒暄、无效内容 | 75,277 | 59.0% |
| negative | 负面回复、不良影响 | 6,087 | 4.8% |

一条 dialog 可同时标注多个 tags，覆盖全部三份数据的 127,540 条对话。

## 六、项目文件结构

```
data-annotation/
├── README.md                    # 项目说明
├── GIT_WORKFLOW.md              # Git工作流规范
├── annotation-specification.docx # 标注规范（原始）
├── course-project-cover.docx    # 课程封面
├── .gitignore
├── data/
│   ├── No-01~03.json            # 原始数据（各8366条）
│   └── 人工标注/
│       ├── annotation-specification.md  # 标注规范文档
│       ├── No-01_待标注_3000.json        # 待标注样本
│       ├── No-01_待标注_3000_已标注.json # 人工标注（3000条）
│       ├── No-01~03_最终版.json          # 最终标注结果
│       ├── No-01~03_带标签.json          # 含dialog tags
│       └── final_stage*.pkl             # 模型文件
├── pipeline/                    # 所有流水线脚本
│   ├── pipeline_a.py            # 思路A：初始标注
│   ├── two_stage.py             # 两阶段分类
│   ├── final_pipeline.py        # 最终版流水线
│   ├── refine_loop.py           # 修正循环
│   ├── auto_correction.py       # 启发式自动修正
│   ├── auto_correct.py          # LLM自动修正
│   ├── generate_review.py       # 审核CSV生成
│   ├── retrain.py               # 加权重训
│   └── ...                      # 其他辅助脚本
├── web/                         # Web工具
│   ├── index.html               # 标注工具（在线改标签）
│   ├── retrain.py               # 重训脚本
│   └── retrain_v2.py            # 重训v2（合并修正）
└── tools/
    └── extract_qa.py
```

## 七、结论

### 7.1 成果

1. **完整标注数据集**：No-01~03 三份数据全部完成分类标注，31/31类全覆盖，S3全部5子类覆盖
2. **多版本模型对比**：LR / NB / SVM 三种模型在S层级分类上的对比，LR以81.06%准确率最优
3. **两阶段分类架构**：Stage1 层级分类（81%）+ Stage2 子类分类（50~67%）+ 关键词兜底，有效提升分类质量
4. **人工修正迭代**：通过Web工具完成3351条修正标注，修正样本加权重训后模型分布更接近人工标注
5. **Dialog Tags**：完成127,540条对话的 knowledge/negative/meaningless 标注

### 7.2 改进方向

1. **S1子类准确率偏低（49%）**：17类过多，部分类目边界模糊（如1.7压力 vs 1.13低自尊），需更多训练数据或特征增强
2. **S3稀有类识别困难**：3.1（正在自杀）在测试集中仅4条，模型无法有效学习，需针对性增强
3. **两阶段误差传递**：Stage1的19%误差会直接导致Stage2分类失败，可通过集成模型降低

---

**报告日期：** 2026年5月31日
